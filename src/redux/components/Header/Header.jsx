import React from 'react';
import { StHeader, StP } from './styles';

const Header = () => {
    return (
        <StHeader>
            <StP>TODOLIST</StP>
        </StHeader>
    );
};

export default Header;
